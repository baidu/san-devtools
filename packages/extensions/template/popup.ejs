<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <style>
            body {
                font-size: 14px;
                min-width: 300px;
                min-height: 50px;
            }
            img {
                float: left;
                margin-right: 10px;
                height: 54px;
                filter: grayscale(100%);
            }
            #version {
                overflow: hidden;
                height: 60px;
            }
        </style>
    </head>
    <body>
        <a href="<%= homepage %>" target="_blank">
            <img src="/icons/logo.svg" />
        </a>
        <p id="version">
            <br />
            San not detected
            <br />
        </p>
    </body>
</html>
